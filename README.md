BMC ADDM TPL Syntax file for Kate
=================================

A BMC ADDM TPL syntax file for the Kate text editor for Linux (default in KDE).

Install by copying addm.xml to:

    ~/.kde/share/apps/katepart/syntax
